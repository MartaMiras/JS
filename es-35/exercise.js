function canPlay() {
  let sport = "Football";
  let personName; // Declarar personName fuera del bloque if
  if (true) {
    personName = "Cosimo"; // Asigne un valor a personName dentro del bloque if
  }
  console.log(personName + sport);
}
canPlay();
